const { React } = replugged.common;
const pplMoeStore = (await import('../profileStore.js')).default;

export default function Profile({ profile }) {
  return React.createElement("div", null,
    React.createElement("span", null, "wgat")
  )
}
